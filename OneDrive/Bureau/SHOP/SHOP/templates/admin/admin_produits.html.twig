{% extends "base.html.twig" %}

{% block body %}

<a href="{{ path('admin_new_produit') }}" class="btn btn-success my-2">Ajouter un produit</a>

<table class="table">
<thead>
<tr>
{%for data in champs %}
<th>{{ data }} </th>
{% endfor %}


<th>Edit</th>
<th>supp</th>
</tr>
</thead>

<tbody>
{% for produit in produits %}
<tr>
<td>{{ produit.id }}</td>
<td>{{ produit.titre }}</td>
<td>{{ produit.description|raw }}</td>
<td>{{ produit.couleur }}</td>
<td>{{ produit.taille }}</td>
<td>{{ produit.collection }}</td>
<td><img src="{{ produit.photo }}" class="img-fluid" width="100" height="100"></td>
<td>{{ produit.prix }}</td>
<td>{{ produit.stock }}</td>

<td>{{ produit.DateEnregistrement|date('d/m/y à H:i:s')}}</td>


<td>

<a href="{{ path('admin_edit_produit',{id: produit.id}) }}" class="text-success">
<i class="fas fa-edit fa-2x"></i>
</a>
</td>

<td>
<a href="{{ path('admin_delete_produit',{id: produit.id}) }}" class="text-danger">
<i class="fas fa-trash fa-2x"></i> 
</a>
</td>
</tr>

{% endfor %}
</tbody>
</table>

{% endblock %}



